#include <stdio.h>

int	ft_isalpha(int i)
{
	if (('a' <= i && i <= 'z') || ('A' <= i && i <= 'Z'))
		return (1);
	return (0);
}

int main(void)
{
    char c1 = 'G';
    char c2 = 'k';
    char c3 = '5';

    printf("%c is: %d\n", c1, ft_isalpha(c1));
    printf("%c is: %d\n", c2, ft_isalpha(c2));
    printf("%c is: %d\n", c3, ft_isalpha(c3)); 

    return 0;
}
